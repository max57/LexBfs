package LexBFS.algorithms.increaseAlgorithm.test;

public class MainTwo {
    public static void main(String[] args) {
        /*VertexService vertexService = new VertexService();
        GraphService createGraphService = new GraphService();
        List<Vertex> vertexList = createGraphService.firstGraph();

        Vertex e = new Vertex("e");


        List<Vertex> output = new LinkedList<Vertex>();

        Vertex start = e;
        start.setValue(vertexList.size());
        for (Vertex vertex : vertexList) {
            if (!start.getName().equals(vertex.getName())){
                vertex.setValue(0);
            } else {
                vertex.setValue(vertexList.size());
            }
        }

        for (int i = 0; i < vertexList.size(); i++){
            Vertex u = vertexService.getVertexWithMaxValue(vertexList);
            u.setVisited(true);
            output.add(u);
            vertexService.increaseUnvisitedNeighboursLabel(u, vertexList.size() - i);
        }

        for (Vertex vertex : output) {
            System.out.println(vertex.getName());
        }*/
    }
}
